{{ form_start(form) }}
    <div class="row">
        <div class="col-12">
            {{ form_row(form.apercu) }}
        </div>
        <div class="col-12">
            {{ form_row(form.titre) }}
        </div>
        <div class="col-12 col-md-6">
            {{ form_row(form.description) }}
        </div>
        <div class="col-12 col-md-6">
            {{ form_row(form.categorie) }}
        </div>
        <div class="col-12 col-md-3">
            {{ form_row(form.photo) }}
        </div>
        <div class="col-12 col-md-3">
            {{ form_row(form.photo2) }}
        </div>
        <div class="col-12 col-md-3">
            {{ form_row(form.photo3) }}
        </div>
        <div class="col-12 col-md-3">
            {{ form_row(form.photo4) }}
        </div>
        <div class="col-6">
            <button class="btn alime-btn btn-2 mt-15">{{ button_label|default("Ajouter l'article") }}</button>
        </div>
    </div>
{{ form_end(form) }}
