<form method="post" action="{{ path('commande_produit_delete', {'id': commande_produit.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir le supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_produit.id) }}">
    <button class="btn">Delete</button>
</form>
